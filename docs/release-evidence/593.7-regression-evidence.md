# gpt-image-mcp-593.7 — Final regression & release preparation evidence
Date: 2026-08-19

## 1. bun test (full suite)
- 667 pass / 0 fail, 1151 expect() calls across 44 files
- Coverage: 90.52% functions / 97.78% lines (meets >=90% threshold)

## 2. Direct tsc (bypassing package.json wrapper script issues)
- `tsc -p tsconfig.build.json --noEmit` — no errors
- `tsc -p tsconfig.build.json` (build) — no errors, dist/ emitted cleanly
- Note: `bun run type-check` / `bun run build` failed in this *sandbox* only
  because a stray `node` shim on PATH (a goose dev-environment hermit wrapper,
  unrelated to this repo) swallows exec failures and resolves to a broken
  hermit-local `node_modules`. Running the identical tsc invocation with
  `/usr/bin/node` (a real Node 22 binary) succeeds with zero diagnostics.
  This is an environment artifact of the sandbox, not a repo defect.

## 3. ESLint
- `eslint 'src/**/*.ts' 'test/**/*.ts'` (exact package.json script): 0 errors,
  96 warnings — all pre-existing `@typescript-eslint/no-explicit-any` in test
  mock helpers. No new lint errors introduced.

## 4. bin/quality-gate.sh from /tmp
- `cd /tmp && PATH="/usr/bin:$PATH" bin/quality-gate.sh` → all 4 gates
  (lint, type-check, test, build) pass, exit 0. Confirms the gate resolves
  PROJECT_ROOT correctly regardless of caller cwd (per gpt-image-mcp-593.6).

## 5. npm pack --dry-run
- Package: @bioinfornatics/gpt-image-mcp@0.1.4
- 121 files, package size 77.2 kB, unpacked 316.2 kB
- Confirmed included: dist/**/*.js, dist/**/*.d.ts, dist/main.js, bin/start.sh,
  docs/authentication/**/*.md, docs/adr/007-*.md, infra/**/*, README.md, LICENSE
- `bin.gpt-image-mcp` → `./dist/main.js` present and correct

## 6. dist/main.js executable inspection
- Shebang: `#!/usr/bin/env node`
- Permissions: `-rwxr-xr-x` (chmod +x applied by build script)
- `file`: "Node.js script executable, ASCII text"
- `node dist/main.js --version` → `gpt-image-mcp 0.1.4`
- `node dist/main.js --help` → correct usage text, lists --check-config,
  --show-config-sources, provider/deployment/transport/port/log-level flags,
  explicitly rejects --api-key / --mcp-api-key with guidance to use
  --api-key-file / --mcp-api-key-file

## 7. git diff --check (working tree + staged)
- No whitespace errors in either diff.

## 8. Cross-provider / cross-transport / security review (code inspection)
- OpenAI: unaffected, existing strategy paths untouched by this task.
- Azure gpt-image-2: exact-match resolved via azure-deployment-registry.ts.
- Azure MAI/unresolved deployments: fail fast with `model-family-unresolved`
  before any generate/edit call; provider_validate/provider_list remain
  reachability-only and decoupled from the registry (by design, documented
  in source comments).
- Custom provider: response_format negotiation correctly distinguishes
  dall-e-* (legacy, needs explicit b64_json) vs gpt-image-compatible
  (base64 by default, must NOT send legacy response_format).
- Together: uses its own strategy, unaffected.
- HTTP transport: default authMode = static_bearer (IMAGE_MCP_API_KEY
  required) unless Azure OBO is configured (-> entra).
- stdio transport: default authMode = none (trusted local host boundary);
  config validation rejects any other authMode for stdio at startup.
- Image media validation (image-media.ts): magic-byte sniffing for
  PNG/JPEG/WebP, rejects mismatched declared MIME vs actual bytes,
  malformed base64, and corrupt/unsupported image data.
- Config resolution precedence: CLI > canonical IMAGE_* env > legacy alias >
  default, verified in config-resolver.ts + its spec; provenance/diagnostics
  never leak secret values.

## Verdict
No release-blocking defects found. No implementation changes made (per task
guidance: fix only if a concrete blocking defect is found). CHANGELOG.md
updated with concise, accurate release/migration notes covering: image media
validation, Azure single IMAGE_DEPLOYMENT + unresolved MAI/others behavior,
CLI/help/config precedence, custom response-format negotiation, and auth mode
defaults per transport.
