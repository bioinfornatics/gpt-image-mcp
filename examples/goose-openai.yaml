extensions:
  imagemcp:
    enabled: true
    type: stdio
    name: Image MCP
    cmd: npx
    args:
      - --yes
      - "@bioinfornatics/image-mcp@0.2.0"
      - --provider
      - openai
      - --transport
      - stdio
    env_keys:
      - IMAGE_API_KEY
    timeout: 300
